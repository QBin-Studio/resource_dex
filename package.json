{
  "name": "resource_dex_root",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run t:dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "check-types": "turbo run check-types",
    "build:dep": "turbo run t:package"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "prettier-plugin-svelte": "^3.3.3",
    "turbo": "^2.5.2"
  },
  "packageManager": "^bun@1.2.20",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*"
  ],
  "trustedDependencies": [
    "@tailwindcss/oxide"
  ]
}